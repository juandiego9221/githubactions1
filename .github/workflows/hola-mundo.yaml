name: learnign github actions
run-name: learning github actions
on: [push]
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: definiri variable
        run: echo "USERNAME=${{github.actor}}" >> $GITHUB_ENV
      - name: correr script
        run: python example.py





